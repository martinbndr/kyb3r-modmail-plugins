# Plugin: dm
This plugin implements a command to send a DM to a specified user.

Keep in mind that your bot can be blocked by discord under certain cases for sending many DM´s to users. Use it with caution.

By default the command is locked to the OWNER Level for security reasons.

## Installation command:
```
?plugin load martinbndr/kyb3r-modmail-plugins/dm@master
```